{
    "train_batch_size": 128,
    "train_micro_batch_size_per_gpu": 16,
    "optimizer": {
      "type": "AdamW",
      "params": {
        "lr": 2e-4,
        "betas": [0.9, 0.95],
        "eps": 1e-8,
        "weight_decay": 0.1
      }
    },
    "scheduler": {
      "type": "WarmupCosineLR",
      "params": {
        "warmup_num_steps": 100,
        "total_num_steps": 1563
      }
    },
    "gradient_clipping": 1.0,
    "bf16": { "enabled": true },
    "zero_optimization": { "stage": 2 }
  }
  